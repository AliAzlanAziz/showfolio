import { JobModeType } from "../enums/jobModeType.enum";

export class ProjectModel {
    id: string;
    user: string;
    title: string;
    description: string;
    workDone: string;
    from: string;
    to: string;
}